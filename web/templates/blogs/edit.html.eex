<h2>Edit blogs</h2>

<%= render "form.html", changeset: @changeset,
                        action: blogs_path(@conn, :update, @blogs) %>

<span><%= link "Back", to: blogs_path(@conn, :index) %></span>
